{
  "name": "easy-game-firebase-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "eslint .",
    "serve": "npx -y firebase-tools@latest emulators:start --only functions,firestore",
    "deploy": "npx -y firebase-tools@latest deploy --only functions,firestore"
  },
  "dependencies": {
    "ethers": "^6.15.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  }
}
