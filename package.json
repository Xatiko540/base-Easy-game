{
  "scripts": {
    "compile": "hardhat compile && node scripts/sync-app-artifacts.js",
    "clean": "hardhat clean",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:ganache": "hardhat run scripts/deploy.js --network localhost",
    "smoke:ganache": "hardhat run scripts/smoke-ganache.js --network localhost",
    "test:contracts": "hardhat test test/EasyGame.js",
    "serve:web:base": "node scripts/serve-web-with-base-headers.js"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.26.0"
  }
}
